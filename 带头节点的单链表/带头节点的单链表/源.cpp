#include"Linklist.h"
#include<iostream>
using namespace std;
int main() {
	/*Linklist<int> list(-1);
	for (int i = 0; i < 10; i++)
		list.Insert(i+1, i + 1);
	int num;
	list.remove(2, num);*/
	int i = 0;
	for (i = 0; i < 3; i++)
	{
		if (1 == i)continue;
		cout << i;
	}
	return 0;

	
}